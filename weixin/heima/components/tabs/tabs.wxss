.tabs_title {
  display: flex;
}

.tabs_title .tab {
  padding: 20rpx 0;
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
}

.tabs_title .active {
  color: var(--themeColor);
  border-bottom: 5rpx solid var(--themeColor);
}
